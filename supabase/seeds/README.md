Seed desabilitado por padrao na Fase 1.

Regras:
- usar `supabase/seeds/*.sql` apenas para bootstrap controlado;
- nunca inserir mocks como base de produto;
- qualquer seed operacional deve ser auditavel e revisado por migration/contrato.
