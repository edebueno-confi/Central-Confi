# packages/contracts

Local reservado para contratos tipados entre backend e frontend.

Regras:

- contratos descrevem payloads, filtros, DTOs e read models;
- contratos não carregam regra de negócio;
- o banco e as funções de domínio continuam sendo a fonte da verdade.
